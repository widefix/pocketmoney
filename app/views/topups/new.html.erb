Topup account: <%= link_to account, account_path(account) %>
<br>
Account balance: <%= Account.new(account).balance %>
<br>

<%= form_tag(account_topup_path(account), method: :post) do |f| %>
  <%= text_field_tag :amount %>
  <%= submit_tag 'save' %>
<% end %>
