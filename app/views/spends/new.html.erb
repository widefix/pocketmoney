Spend from account: <%= link_to account.name, account_path(account) %>
<br>
Account balance: <%= account.balance %>
<br>

<%= form_tag(account_spend_path(account), method: :post) do |f| %>
  <label>
    Amount
    <%= text_field_tag :amount %>
  </label>
  <label>
    Description
    <%= text_field_tag :description %>
  </label>
  <%= submit_tag 'save' %>
<% end %>
