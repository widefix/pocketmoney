Topup account: <%= link_to account.name, account_path(account) %>
<br>
Account balance: <%= account.balance %>
<br>

<%= form_tag(account_topup_path(account), method: :post) do |f| %>
  <%= text_field_tag :amount %>
  <%= submit_tag 'save' %>
<% end %>
