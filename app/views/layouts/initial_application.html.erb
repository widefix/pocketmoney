<!DOCTYPE html>
<html lang='en'>
  <head>
    <%= render 'layouts/head'%>
  </head>

  <body>
      <%= render 'layouts/header/initial' %>
    <div class="container hero is-fullheight bottom-wrapper " >
      <section class="section is-fullheight py-0 vcentered-columns">
        <%= yield %>
      </section>
      <%= render 'layouts/footer/footer'%>
    </div>
  </body>
</html>
